import React  from 'react';
import "./style.css";

// We gonna spread all the props into the passed elements

function DeleteBtn(props) {
   render()

   return(
       < span className = "Delete-btn" {...props} type="button" tabindex="0">
       </span>
   );
}
   
export default DeleteBtn;

